{
  "name": "colab-sandbox-mcp",
  "version": "1.0.0",
  "description": "MCP server — Google Colab as a Linux sandbox via webterm tunnel",
  "type": "module",
  "bin": { "colab-sandbox-mcp": "./dist/index.js" },
  "scripts": {
    "build": "tsc",
    "dev": "node --watch dist/index.js",
    "start": "node dist/index.js",
    "prepare": "tsc"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}